#include<bits/stdc++.h>
#define isocyanide ios::sync_with_stdio(false); cin.tie(0), cout.tie(0);
#define all(x) (x).begin(), (x).end()

using namespace std;
typedef vector<int> vi;

void Solve() {
    std::array<int,10> ids;
    std::fill(all(ids),1024);

    for(int& id : ids) {
        std:: cout << id << '\n';
    }
}

int32_t main() {
    
    isocyanide
    
    Solve();

    return 0;
}